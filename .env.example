# Build-time only — values are baked into environment.*.ts via build replace.
# Meta-compose host ports:
GRAPHQL_URL=http://localhost:8093/graphql
DOCUMENTS_API_URL=http://localhost:8091/api/v1
AI_API_URL=http://localhost:8092/api/v1
